<div class="container">
  <h1>Ingredients/Doses</h1>
  <div class="card">
    <%= simple_form_for([@cocktail, @dose]) do |f| %>
      <%= f.input :description %>
      <%= f.association :ingredient %>
      <%= f.submit class:'btn btn-primary'%>

    <% end %>


  </div>
</div>
